export const config = {
  apiOrigin: process.env.CRN_API_URL || 'https://api-dev.hub.asap.science',
  appUrl: process.env.CRN_APP_URL || 'https://dev.hub.asap.science',
  environment: process.env.ENVIRONMENT || 'dev',
};
