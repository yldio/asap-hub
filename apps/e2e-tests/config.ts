export const config = {
  apiOrigin: process.env.API_URL || 'https://api-dev.hub.asap.science',
  appUrl: process.env.APP_URL || 'https://dev.hub.asap.science',
  environment: process.env.ENVIRONMENT || 'dev',
};
