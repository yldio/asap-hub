import { BiographyModal } from '@asap-hub/react-components';

export default {
  title: 'Templates / User Profile / Biography Modal',
  component: BiographyModal,
};

export const Normal = () => <BiographyModal biography="My Bio" backHref="#" />;
