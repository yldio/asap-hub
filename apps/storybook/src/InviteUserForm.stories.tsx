import React from 'react';

import { InviteUserForm } from '@asap-hub/react-components';

export default { title: 'Templates / Admin / Invite User Form' };

export const Normal = () => <InviteUserForm />;
