import React from 'react';

import { RecordOutputForm } from '@asap-hub/react-components';

export default { title: 'Templates / Record Output Form' };

export const Normal = () => <RecordOutputForm />;
