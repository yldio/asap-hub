import { SharedOutputDropdown } from '@asap-hub/react-components';

export default {
  title: 'Organisms / Shared Output Dropdown',
  component: SharedOutputDropdown,
};

export const Normal = () => <SharedOutputDropdown />;
