import React from 'react';
import { UserMenuButton } from '@asap-hub/react-components';

export default {
  title: 'Molecules / Navigation / User Menu Button',
  component: UserMenuButton,
};

export const Normal = () => <UserMenuButton />;
