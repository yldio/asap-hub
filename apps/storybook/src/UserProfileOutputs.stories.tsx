import { UserProfileOutputs } from '@asap-hub/react-components';

export default {
  title: 'Templates / User Profile / Outputs',
  component: UserProfileOutputs,
};

export const Normal = () => <UserProfileOutputs />;
