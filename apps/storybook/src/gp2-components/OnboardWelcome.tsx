import { OnboardWelcome } from '@asap-hub/gp2-components';

export default {
  title: 'GP2 / Templates / Onboard Welcome',
  component: OnboardWelcome,
};

export const Normal = () => <OnboardWelcome />;
