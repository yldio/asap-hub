import { Theme } from '@asap-hub/gp2-components';

export default {
  title: 'GP2 / Templates / Theme',
  component: Theme,
};

export const Normal = () => (
  <Theme>
    <h1>Title</h1>
    <p>Description</p>
  </Theme>
);
