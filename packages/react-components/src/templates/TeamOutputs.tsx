import React from 'react';
import { ComingSoon } from '../organisms';

const TeamOutputs: React.FC<{}> = () => (
  <ComingSoon>
    As each team creates and shares more research outputs such as datasets,
    protocols, software and other resources they will appear here.
  </ComingSoon>
);

export default TeamOutputs;
